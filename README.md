# اسکنر فرکانس میکروتیک

برنامه Windows Forms حرفه‌ای برای پیدا کردن بهترین فرکانس در روترهای میکروتیک QRT5 که به صورت Point-to-Point به هم متصل شده‌اند.

## ✨ ویژگی‌ها

### اتصال و ارتباط
- ✅ اتصال امن به روتر میکروتیک از طریق Telnet
- ✅ مدیریت خودکار اتصال و قطع ارتباط
- ✅ تشخیص و مدیریت خطاهای اتصال

### اسکن فرکانس
- ✅ دریافت و نمایش وضعیت فعلی روتر (base) قبل از شروع اسکن
- ✅ اسکن خودکار رنج فرکانس با پرش قابل تنظیم
- ✅ تنظیم فرکانس یکی یکی به صورت خودکار
- ✅ انتظار برای استیبل شدن بعد از هر تغییر فرکانس (قابل تنظیم به دقیقه)
- ✅ نمایش پیشرفت اسکن با Progress Bar
- ✅ نمایش ردیف base با رنگ آبی در Grid برای تشخیص آسان

### جمع‌آوری داده
- ✅ جمع‌آوری پارامترهای مهم:
  - توان آنتن (Antenna Power)
  - سرعت دانلود و آپلود (Download/Upload Speed)
  - نسبت سیگنال به نویز (SNR)
  - قدرت سیگنال (Signal Strength)
  - سطح نویز (Noise Floor)
  - CCQ (Client Connection Quality)
  - Tx Rate و Rx Rate

### ذخیره و مدیریت داده
- ✅ ذخیره خودکار نتایج در فایل JSON با فرمت ساختاریافته
- ✅ بارگذاری نتایج قبلی از فایل‌های JSON
- ✅ ذخیره خودکار تنظیمات در فایل `settings.json`

### رابط کاربری
- ✅ رابط کاربری فارسی و کاربرپسند
- ✅ نمایش نتایج در Grid با قابلیت مرتب‌سازی
- ✅ فرمت‌دهی خودکار اعداد و تاریخ‌ها
- ✅ نمایش وضعیت لحظه‌ای اسکن
- ✅ اعتبارسنجی کامل تنظیمات قبل از شروع

## 📋 نحوه استفاده

### 1. تنظیمات اولیه

در تب "تنظیمات":
- **آدرس IP روتر**: آدرس IP روتر میکروتیک (مثال: 192.168.88.1)
- **نام کاربری**: نام کاربری برای اتصال Telnet
- **رمز عبور**: رمز عبور برای اتصال Telnet
- **فرکانس شروع**: شروع رنج فرکانس (MHz)
- **فرکانس پایان**: پایان رنج فرکانس (MHz)
- **پرش فرکانس**: فاصله بین هر فرکانس (MHz)
- **زمان استیبل شدن**: زمان انتظار بعد از تغییر فرکانس (دقیقه)
- **نام اینترفیس**: نام اینترفیس وایرلس (معمولاً wlan1)

### 2. شروع اسکن

1. روی دکمه "ذخیره تنظیمات" کلیک کنید (اختیاری)
2. روی دکمه "شروع اسکن" کلیک کنید
3. برنامه به صورت خودکار:
   - به روتر متصل می‌شود
   - **وضعیت فعلی روتر را دریافت و با Status="base" ذخیره می‌کند**
   - فرکانس‌ها را یکی یکی تنظیم می‌کند
   - منتظر استیبل شدن می‌ماند
   - اطلاعات را جمع‌آوری می‌کند
   - نتایج را در Grid نمایش می‌دهد
   - نتایج را در فایل JSON ذخیره می‌کند

### 3. مشاهده نتایج

- نتایج به صورت لحظه‌ای در تب "نتایج" نمایش داده می‌شوند
- **ردیف base**: وضعیت فعلی روتر قبل از شروع اسکن با رنگ آبی روشن نمایش داده می‌شود
- می‌توانید ستون‌ها را مرتب کنید
- می‌توانید نتایج قبلی را با دکمه "بارگذاری نتایج قبلی" بارگذاری کنید
- وضعیت‌های مختلف:
  - **base**: وضعیت فعلی روتر (آبی)
  - **موفق**: اسکن موفق (سفید)
  - **خطا**: خطا در اسکن (قرمز)

## 🔧 نیازمندی‌ها

- .NET 8.0 یا بالاتر
- Windows 10 یا بالاتر
- دسترسی Telnet به روتر میکروتیک
- روتر میکروتیک با RouterOS

## 📁 ساختار پروژه

```
Ntk.Mikrotik.Tools/
├── MainForm.cs                    # فرم اصلی با UI
├── Program.cs                     # نقطه ورود برنامه
├── Models/
│   ├── FrequencyScanResult.cs     # مدل نتیجه اسکن
│   └── ScanSettings.cs            # مدل تنظیمات
├── Services/
│   ├── MikroTikTelnetClient.cs    # کلاس اتصال Telnet
│   ├── FrequencyScanner.cs        # کلاس اسکنر فرکانس
│   ├── JsonDataService.cs         # مدیریت JSON
│   └── SettingsValidator.cs       # اعتبارسنجی تنظیمات
└── README.md                       # این فایل
```

## ⚙️ تنظیمات RouterOS

قبل از استفاده، مطمئن شوید:

1. **فعال کردن Telnet**:
   ```
   /ip service enable telnet
   ```

2. **بررسی اینترفیس وایرلس**:
   ```
   /interface wireless print
   ```

3. **بررسی دسترسی کاربر**:
   مطمئن شوید کاربر مورد نظر دسترسی Telnet دارد

## 📝 نکات مهم

- ⚠️ زمان استیبل شدن باید به اندازه کافی باشد تا اتصال پایدار شود (حداقل 2 دقیقه توصیه می‌شود)
- ⚠️ نتایج در پوشه `ScanResults` در کنار فایل اجرایی ذخیره می‌شوند
- ⚠️ تنظیمات در فایل `settings.json` ذخیره می‌شوند
- ⚠️ در طول اسکن، اتصال وایرلس ممکن است موقتاً قطع شود
- ✅ می‌توانید اسکن را در هر زمان با دکمه "توقف" متوقف کنید

## 🐛 عیب‌یابی

### مشکل در اتصال
- بررسی کنید Telnet روی روتر فعال است
- بررسی کنید IP، نام کاربری و رمز عبور صحیح است
- بررسی کنید فایروال مانع اتصال نمی‌شود

### مشکل در جمع‌آوری داده
- بررسی کنید نام اینترفیس صحیح است
- بررسی کنید اینترفیس وایرلس فعال است
- بررسی کنید اتصال Point-to-Point برقرار است

## 📄 مجوز

این پروژه برای استفاده شخصی و تجاری آزاد است.

## 👨‍💻 توسعه

برای ساخت پروژه:
```bash
dotnet restore
dotnet build
dotnet run
```

برای انتشار:
```bash
dotnet publish -c Release
```

