# 📡 MikroTik Frequency Scanner Tool

<p align="center">
  <img src="https://img.shields.io/badge/Platform-Windows%2010%2B-blue" />
  <img src="https://img.shields.io/badge/.NET-8.0-5C2D91" />
  <img src="https://img.shields.io/badge/UI-WinForms-1793d1" />
  <img src="https://img.shields.io/badge/Protocol-SSH.NET-brightgreen" />
  <img src="https://img.shields.io/badge/Language-Persian%20(UI)-orange" />
</p>

<div dir="rtl">

## 🚀 خلاصه خیلی کوتاه
- ابزار دسکتاپ (WinForms) برای اسکن و بهینه‌سازی لینک‌های Point-to-Point میکروتیک.
- اتصال امن SSH، اسکن خودکار فرکانس/پروتکل/Channel Width، و ثبت کامل Registration Table با `stat`.
- ذخیره نتایج در JSON، نمایش در گرید با فیلتر و مرتب‌سازی، و دکمه «وضعیت» برای دریافت فوری وضعیت لینک.

</div>

---

## ✨ ویژگی‌های کلیدی

### 🔌 اتصال و ارتباط
- ✅ **اتصال امن از طریق SSH** به روترهای MikroTik
- ✅ **مدیریت خودکار اتصال** و قطع ارتباط
- ✅ **تشخیص و مدیریت خطاهای اتصال** به صورت خودکار
- ✅ **اتصال مجدد خودکار** در صورت قطع ارتباط
- ✅ **لاگ کامل ترمینال** برای دیباگ و ردیابی

### 📊 اسکن فرکانس پیشرفته
- ✅ **دریافت وضعیت فعلی** روتر (base) قبل از شروع اسکن
- ✅ **اسکن خودکار رنج فرکانس** با پرش قابل تنظیم
- ✅ **تنظیم فرکانس به صورت خودکار** یکی یکی
- ✅ **انتظار برای استیبل شدن** بعد از هر تغییر فرکانس (قابل تنظیم)
- ✅ **پشتیبانی از چندین پروتکل** (nstreme, nv2, 802.11)
- ✅ **پشتیبانی از چندین Channel Width** (20MHz, 40MHz, و غیره)
- ✅ **اسکن ترکیبی** از تمام ترکیبات فرکانس، پروتکل و channel width
- ✅ **نمایش پیشرفت اسکن** با Progress Bar و درصد پیشرفت

### 📈 جمع‌آوری داده‌های جامع

#### اطلاعات آنتن محلی (Local Antenna)
- ✅ توان آنتن (Antenna Power)
- ✅ سرعت دانلود و آپلود (Download/Upload Speed)
- ✅ نسبت سیگنال به نویز (SNR)
- ✅ قدرت سیگنال (Signal Strength)
- ✅ سطح نویز (Noise Floor)
- ✅ CCQ (Client Connection Quality)
- ✅ Tx Rate و Rx Rate
- ✅ Band و Channel Width
- ✅ Wireless Protocol

#### اطلاعات آنتن Remote (متصل شده)
- ✅ **اطلاعات کامل Registration Table** از کامند `stat`
- ✅ MAC Address و Identity
- ✅ Radio Name
- ✅ Signal Strength و SNR
- ✅ Tx Rate و Rx Rate
- ✅ **CCQ تفکیک شده**: Tx CCQ و Rx CCQ
- ✅ P-Throughput
- ✅ Signal Strength برای کانال‌های 0 و 1
- ✅ Tx Signal Strength برای کانال‌های 0 و 1
- ✅ **آمار کامل شبکه**:
  - Packets (Rx/Tx)
  - Bytes (Rx/Tx)
  - Frames (Rx/Tx)
  - Frame Bytes (Rx/Tx)
  - Hardware Frames (Rx/Tx)
  - Hardware Frame Bytes (Rx/Tx)
  - Tx Frames Timed Out
- ✅ Uptime و Last Activity
- ✅ Nstreme و Nstreme Plus
- ✅ RouterOS Version
- ✅ Last IP
- ✅ اطلاعات امنیتی (802.1x, Authentication, Encryption, و غیره)
- ✅ WMM Enabled و Compression

### 💾 ذخیره و مدیریت داده
- ✅ **ذخیره خودکار نتایج** در فایل JSON با فرمت ساختاریافته
- ✅ **بارگذاری نتایج قبلی** از فایل‌های JSON
- ✅ **ذخیره خودکار تنظیمات** در فایل `settings.json`
- ✅ **قابلیت بازگشت به تنظیمات پیش‌فرض**
- ✅ **مدیریت فایل‌های اسکن** با تاریخ و زمان

### 🎨 رابط کاربری حرفه‌ای
- ✅ **رابط کاربری فارسی** و کاربرپسند
- ✅ **نمایش نتایج در Grid** با قابلیت مرتب‌سازی پیشرفته
- ✅ **فرمت‌دهی خودکار** اعداد و تاریخ‌ها
- ✅ **نمایش وضعیت لحظه‌ای** اسکن
- ✅ **اعتبارسنجی کامل** تنظیمات قبل از شروع
- ✅ **رنگ‌بندی هوشمند** نتایج (base: آبی، موفق: سفید، خطا: قرمز)
- ✅ **دکمه وضعیت** برای دریافت سریع وضعیت فعلی آنتن
- ✅ **فیلتر و جستجو** در نتایج
- ✅ **لاگ ترمینال** برای مشاهده تمام کامندها و پاسخ‌ها

### ⚙️ قابلیت‌های پیشرفته
- ✅ **قابلیت توقف** اسکن در هر زمان
- ✅ **بازگردانی خودکار تنظیمات** به حالت اولیه بعد از اسکن
- ✅ **پشتیبانی از کامندهای قابل تنظیم** RouterOS
- ✅ **پارسی‌سازی کامل** خروجی‌ها و پیام‌ها
- ✅ **مدیریت خطاهای جامع** با پیام‌های واضح

---

## 📋 نیازمندی‌ها

- **.NET 8.0** یا بالاتر
- **Windows 10** یا بالاتر
- **دسترسی SSH** به روتر MikroTik
- **روتر MikroTik** با RouterOS (ترجیحاً نسخه 7.x یا بالاتر)
- **اتصال Point-to-Point** بین دو روتر

---

## 🚀 نصب و راه‌اندازی

### روش 1: استفاده از فایل اجرایی (Recommended)

1. **دانلود آخرین نسخه** از [Releases](../../releases)
2. **استخراج فایل‌ها** در یک پوشه
3. **اجرای `Ntk.Mikrotik.Tools.exe`**

### روش 2: کامپایل از سورس

```bash
# کلون کردن پروژه
git clone https://github.com/yourusername/Ntk.Mikrotik.Tools.git
cd Ntk.Mikrotik.Tools

# بازگردانی پکیج‌ها
dotnet restore

# کامپایل
dotnet build

# اجرا
dotnet run
```

### روش 3: انتشار (Publish)

```bash
# انتشار برای Windows
dotnet publish -c Release -r win-x64 --self-contained false
```

---

## 📖 راهنمای استفاده

### 1️⃣ تنظیمات اولیه

#### تب "اتصال"
- **آدرس IP روتر**: آدرس IP روتر MikroTik (مثال: `192.168.88.1`)
- **پورت SSH**: پورت SSH (پیش‌فرض: `22`)
- **نام کاربری**: نام کاربری برای اتصال SSH
- **رمز عبور**: رمز عبور برای اتصال SSH
- روی دکمه **"اتصال"** کلیک کنید

#### تب "تنظیمات"
- **فرکانس شروع**: شروع رنج فرکانس (MHz) - مثال: `5220`
- **فرکانس پایان**: پایان رنج فرکانس (MHz) - مثال: `5250`
- **پرش فرکانس**: فاصله بین هر فرکانس (MHz) - مثال: `5`
- **زمان استیبل شدن**: زمان انتظار بعد از تغییر فرکانس (دقیقه) - پیشنهاد: `2`
- **نام اینترفیس**: نام اینترفیس وایرلس (معمولاً `wlan1`)
- **پروتکل‌های Wireless**: لیست پروتکل‌ها (جدا شده با کاما یا خط جدید)
  - مثال: `nstreme`, `nv2`, `802.11`
- **Channel Width**: لیست عرض کانال‌ها (جدا شده با کاما یا خط جدید)
  - مثال: `20MHz`, `40MHz`, `20/40MHz-eC`

#### تنظیمات کامندهای RouterOS (پیشرفته)
تمام کامندهای RouterOS قابل تنظیم هستند:
- `CommandGetFrequency`: کامند دریافت فرکانس
- `CommandSetFrequency`: کامند تنظیم فرکانس
- `CommandSetWirelessProtocol`: کامند تنظیم پروتکل
- `CommandSetChannelWidth`: کامند تنظیم عرض کانال
- `CommandGetInterfaceInfo`: کامند دریافت اطلاعات اینترفیس
- `CommandGetRegistrationTable`: کامند دریافت Registration Table (پیشنهاد: استفاده از `stat`)
- `CommandMonitorInterface`: کامند مانیتورینگ اینترفیس

### 2️⃣ شروع اسکن

1. **اتصال به روتر**: روی دکمه "اتصال" کلیک کنید
2. **ذخیره تنظیمات** (اختیاری): روی دکمه "ذخیره تنظیمات" کلیک کنید
3. **شروع اسکن**: روی دکمه "شروع اسکن" کلیک کنید

برنامه به صورت خودکار:
- ✅ به روتر متصل می‌شود
- ✅ **وضعیت فعلی روتر را دریافت و با Status="base" ذخیره می‌کند**
- ✅ تمام ترکیبات فرکانس، پروتکل و channel width را تست می‌کند
- ✅ برای هر فرکانس:
  - فرکانس را تنظیم می‌کند
  - منتظر استیبل شدن می‌ماند
  - اطلاعات را از سه منبع جمع‌آوری می‌کند:
    - Interface Info
    - Registration Table (با کامند `stat`)
    - Monitor Interface
  - نتایج را در Grid نمایش می‌دهد
- ✅ نتایج را در فایل JSON ذخیره می‌کند
- ✅ بعد از اتمام، تنظیمات را به حالت اولیه برمی‌گرداند

### 3️⃣ مشاهده و مدیریت نتایج

#### تب "نتایج"
- **نمایش لحظه‌ای**: نتایج به صورت لحظه‌ای در Grid نمایش داده می‌شوند
- **مرتب‌سازی**: کلیک روی هدر ستون‌ها برای مرتب‌سازی
- **فیلتر**: استفاده از فیلدهای فیلتر برای جستجو
- **رنگ‌بندی**:
  - 🔵 **base**: وضعیت فعلی روتر قبل از شروع اسکن (آبی)
  - ⚪ **موفق**: اسکن موفق (سفید)
  - 🔴 **خطا**: خطا در اسکن (قرمز)
  - 📊 **وضعیت**: نتیجه دکمه "وضعیت" (سبز)

#### دکمه "وضعیت"
- بعد از اتصال، می‌توانید روی دکمه **"وضعیت"** کلیک کنید
- وضعیت فعلی آنتن و آنتن‌های متصل را دریافت می‌کند
- نتایج در Grid نمایش داده می‌شوند
- یک MessageBox با خلاصه اطلاعات نمایش داده می‌شود

#### بارگذاری نتایج قبلی
- روی دکمه **"بارگذاری نتایج قبلی"** کلیک کنید
- فایل JSON مورد نظر را انتخاب کنید
- نتایج در Grid نمایش داده می‌شوند

### 4️⃣ لاگ ترمینال

- تمام کامندهای ارسال شده و پاسخ‌های دریافت شده در تب "ترمینال" نمایش داده می‌شوند
- این لاگ برای دیباگ و ردیابی مشکلات بسیار مفید است

---

## 📁 ساختار پروژه

```
Ntk.Mikrotik.Tools/
├── MainForm.cs                    # فرم اصلی با UI کامل
├── Program.cs                     # نقطه ورود برنامه
├── Models/
│   ├── FrequencyScanResult.cs     # مدل نتیجه اسکن (با تمام فیلدها)
│   └── ScanSettings.cs            # مدل تنظیمات (با کامندهای قابل تنظیم)
├── Services/
│   ├── MikroTikSshClient.cs       # کلاس اتصال SSH به MikroTik
│   ├── FrequencyScanner.cs        # کلاس اصلی اسکنر فرکانس
│   ├── JsonDataService.cs         # مدیریت ذخیره و بارگذاری JSON
│   └── SettingsValidator.cs       # اعتبارسنجی تنظیمات
├── Properties/
│   └── PublishProfiles/           # پروفایل‌های انتشار
├── icon.ico                       # آیکون برنامه
├── app.manifest                   # مانیفست برنامه
└── README.md                      # این فایل
```

---

## 🛠️ تکنولوژی‌های استفاده شده

- **.NET 8.0**: Framework اصلی
- **Windows Forms**: رابط کاربری
- **SSH.NET (2023.0.0)**: اتصال SSH به MikroTik
- **Newtonsoft.Json (13.0.3)**: مدیریت JSON
- **C# 12**: زبان برنامه‌نویسی

---

## ⚙️ تنظیمات RouterOS

قبل از استفاده، مطمئن شوید:

### 1. فعال کردن SSH

```bash
/ip service enable ssh
```

### 2. بررسی اینترفیس وایرلس

```bash
/interface wireless print
```

### 3. بررسی دسترسی کاربر

مطمئن شوید کاربر مورد نظر دسترسی SSH دارد:

```bash
/user print
/user set [find name=your-username] group=full
```

### 4. بررسی Registration Table

برای اطمینان از دریافت اطلاعات کامل:

```bash
/interface wireless registration-table print stat where interface="wlan1"
```

---

## 📝 نکات مهم

### ⚠️ هشدارها

- **زمان استیبل شدن**: باید به اندازه کافی باشد تا اتصال پایدار شود (حداقل 2 دقیقه توصیه می‌شود)
- **اتصال موقت**: در طول اسکن، اتصال وایرلس ممکن است موقتاً قطع شود
- **پشتیبان‌گیری**: قبل از شروع اسکن، از تنظیمات فعلی روتر پشتیبان بگیرید
- **دسترسی**: مطمئن شوید کاربر SSH دسترسی کامل به تنظیمات وایرلس دارد

### ✅ توصیه‌ها

- **استفاده از کامند `stat`**: برای دریافت اطلاعات کامل از Registration Table، از کامند `stat` استفاده کنید
- **ذخیره تنظیمات**: بعد از تنظیم، حتماً تنظیمات را ذخیره کنید
- **بررسی لاگ**: در صورت بروز مشکل، لاگ ترمینال را بررسی کنید
- **تست اتصال**: قبل از شروع اسکن، اتصال را تست کنید

---

## 🐛 عیب‌یابی

### مشکل در اتصال

1. **بررسی SSH**: مطمئن شوید SSH روی روتر فعال است
   ```bash
   /ip service print where name=ssh
   ```

2. **بررسی IP و پورت**: IP، پورت، نام کاربری و رمز عبور را بررسی کنید

3. **بررسی فایروال**: مطمئن شوید فایروال مانع اتصال نمی‌شود

4. **بررسی شبکه**: ping به روتر را تست کنید

### مشکل در جمع‌آوری داده

1. **نام اینترفیس**: مطمئن شوید نام اینترفیس صحیح است
   ```bash
   /interface wireless print
   ```

2. **فعال بودن اینترفیس**: مطمئن شوید اینترفیس وایرلس فعال است
   ```bash
   /interface wireless enable wlan1
   ```

3. **اتصال P2P**: مطمئن شوید اتصال Point-to-Point برقرار است

4. **کامند Registration Table**: مطمئن شوید از کامند `stat` استفاده می‌کنید:
   ```
   /interface wireless registration-table print stat where interface="wlan1"
   ```

### مشکل در پارس داده‌ها

1. **بررسی لاگ ترمینال**: لاگ ترمینال را بررسی کنید تا ببینید چه داده‌ای دریافت شده
2. **بررسی فرمت خروجی**: مطمئن شوید فرمت خروجی RouterOS با انتظارات برنامه همخوانی دارد
3. **بررسی نسخه RouterOS**: برخی ویژگی‌ها ممکن است در نسخه‌های قدیمی‌تر موجود نباشند

---

## 📊 نمونه خروجی JSON

```json
{
  "ScanSettings": {
    "RouterIpAddress": "192.168.88.1",
    "InterfaceName": "wlan1",
    "StartFrequency": 5220.0,
    "EndFrequency": 5250.0,
    "FrequencyStep": 5.0
  },
  "ScanTime": "2025-12-15T07:35:48.6687501+03:30",
  "Results": [
    {
      "ScanTime": "2025-12-15T07:35:48.6687501+03:30",
      "Frequency": 5225.0,
      "SignalStrength": -75.0,
      "SignalToNoiseRatio": 34.0,
      "NoiseFloor": -109.0,
      "CCQ": 71.0,
      "RemoteMacAddress": "4C:5E:0C:7F:D4:B1",
      "RemoteSignalStrength": -75.0,
      "RemoteTxCCQ": 45.0,
      "RemoteRxCCQ": 68.0,
      "RemotePThroughput": 11105.0,
      "Status": "base"
    }
  ]
}
```

---

## 🤝 مشارکت

مشارکت‌ها، پیشنهادات و گزارش باگ‌ها خوش‌آمد هستند!

1. Fork کنید
2. یک branch برای feature خود ایجاد کنید (`git checkout -b feature/AmazingFeature`)
3. تغییرات را commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. به branch خود push کنید (`git push origin feature/AmazingFeature`)
5. یک Pull Request باز کنید

---

## 📄 مجوز

این پروژه برای استفاده شخصی و تجاری آزاد است.

---

## 👨‍💻 توسعه

### ساخت پروژه

```bash
dotnet restore
dotnet build
```

### اجرای پروژه

```bash
dotnet run
```

### انتشار

```bash
# Windows x64
dotnet publish -c Release -r win-x64 --self-contained false

# Windows x86
dotnet publish -c Release -r win-x86 --self-contained false
```

### تست

```bash
dotnet test
```

---

## 📞 پشتیبانی

- **Issues**: برای گزارش باگ یا درخواست ویژگی جدید، یک Issue باز کنید
- **Discussions**: برای سوالات و بحث، از Discussions استفاده کنید

---

## 🎯 Roadmap

- [ ] پشتیبانی از چندین روتر همزمان
- [ ] نمودارهای آماری و تجسم داده‌ها
- [ ] Export به Excel و CSV
- [ ] پشتیبانی از API برای یکپارچه‌سازی
- [ ] پشتیبانی از Linux و macOS
- [ ] رابط کاربری وب

---

## ⭐ ستاره دادن

اگر این پروژه برای شما مفید بود، لطفاً یک ستاره ⭐ بدهید!

---

<div align="center">

**ساخته شده با ❤️ برای جامعه MikroTik**

</div>

</div>
